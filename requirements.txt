# ─── 核心依赖 ─────────────────────────────
pyyaml>=6.0
requests>=2.31
Pillow>=10.0
python-dotenv>=1.0

# ─── LLM API ──────────────────────────────
openai>=1.0            # 兼容 DeepSeek / Kimi / MiniMax (OpenAI-compatible API)

# ─── 图像后期处理 ───────────────────────────
opencv-python>=4.8
numpy>=1.24

# ─── 图像生成后端 ──────────────────────────
# replicate>=0.25        # Replicate API

# ─── 可选: 进度显示 ────────────────────────
tqdm>=4.65

# ─── R2 对象存储 (Cloudflare R2 S3 兼容) ──────
boto3>=1.34

# ─── 素材爬取 ─────────────────────────────
ddgs>=6.0              # DuckDuckGo 图片搜索（无 API Key）
